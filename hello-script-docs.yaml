paths:
  /custom-script:
    post:
      summary: "Execute Custom script for testing purposes"
      description: ""
      operationId: "postCustom"
      consumes:
      - "application/json"
      - "application/xml"
      produces:
      - "application/xml"
      - "application/json"
      parameters:
      - in: "body"
        name: "body"
        description: "Pet object that needs to be added to the store"
        required: true
      responses:
        405:
          description: "Invalid input"
